# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dist.zip
*.local

# Environment files — NEVER commit. Contains secrets / DB keys.
# `.env.example` and `.env.production.example` are the documented templates
# (no real values) and must be tracked so future deploys aren't guesswork.
.env
.env.*
!.env.example
!.env.production.example

# Quarantined legacy scripts (kept locally as evidence, not pushed)
_legacy_scripts/

# Supabase CLI link state
supabase/.temp/
supabase/.branches/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
